<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="36dp"
    android:gravity="center"
    android:minHeight="36dp"
    android:orientation="horizontal"
    tools:background="@color/ai_fill7">

    <TextView
        android:id="@+id/tab_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginHorizontal="6dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:textColor="@color/ai_icontext1"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="@string/cov_setting_avatar_all" />

    <View
        android:id="@+id/tab_divider"
        android:layout_width="0dp"
        android:layout_height="2dp"
        android:layout_gravity="end|bottom"
        android:layout_marginTop="2dp"
        app:layout_constraintStart_toStartOf="@id/tab_text"
        app:layout_constraintEnd_toEndOf="@id/tab_text"
        app:layout_constraintTop_toBottomOf="@id/tab_text"
        android:layout_marginHorizontal="6dp"
        android:background="@drawable/cov_tab_divider_bg" />
</androidx.constraintlayout.widget.ConstraintLayout>
