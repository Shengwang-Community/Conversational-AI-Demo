interface:
  display_name: "ac-review"
  short_description: "reviewing Android changes for correctness, regressions and validation gaps"
  default_prompt: "Use $ac-review as needed with Android/AGENTS.md and AI_WORKFLOW.md. Choose an approach that fits the task."
policy:
  allow_implicit_invocation: true
