# Optional lesson note

- Observation and evidence
- Cause and correction
- Where the lesson applies
- Validation and remaining uncertainty

Existing `memory/episodes/`, `memory/pattern-candidates.json`, `memory/semantic-patterns.json` and `memory/working/` may hold useful history. Read or update them only when they help; no fixed output schema is required.
