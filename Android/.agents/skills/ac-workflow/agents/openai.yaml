interface:
  display_name: "ac-workflow"
  short_description: "carrying out or continuing Android work with repository context"
  default_prompt: "Use $ac-workflow as needed with Android/AGENTS.md and AI_WORKFLOW.md. Choose an approach that fits the task."
policy:
  allow_implicit_invocation: true
