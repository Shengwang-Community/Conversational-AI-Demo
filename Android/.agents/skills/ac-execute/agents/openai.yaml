interface:
  display_name: "ac-execute"
  short_description: "implementing or fixing Android code with focused validation"
  default_prompt: "Use $ac-execute as needed with Android/AGENTS.md and AI_WORKFLOW.md. Choose an approach that fits the task."
policy:
  allow_implicit_invocation: true
