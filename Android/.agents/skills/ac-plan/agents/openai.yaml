interface:
  display_name: "ac-plan"
  short_description: "an Android task benefits from an explicit plan or tradeoff analysis"
  default_prompt: "Use $ac-plan as needed with Android/AGENTS.md and AI_WORKFLOW.md. Choose an approach that fits the task."
policy:
  allow_implicit_invocation: true
