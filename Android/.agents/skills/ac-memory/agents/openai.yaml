interface:
  display_name: "AC Memory"
  short_description: "Manage task-state files and the workflow index before work continues"
  default_prompt: "Use $ac-memory to create, validate, and update `.agents/state/INDEX.md` plus the active task state file before planner, executor, or reviewer work continues."
