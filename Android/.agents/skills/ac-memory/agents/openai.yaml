interface:
  display_name: "ac-memory"
  short_description: "a long Android task would benefit from a checkpoint or recovery note"
  default_prompt: "Use $ac-memory as needed with Android/AGENTS.md and AI_WORKFLOW.md. Choose an approach that fits the task."
policy:
  allow_implicit_invocation: true
